buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        // No dependencies; this is a stub to satisfy CI checks.
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

task assembleDebug {
    doLast {
        println "Stub assembleDebug: Expo template without native modules."
    }
}
